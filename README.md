# pysc2_converter_external

This repository contains a converter that transforms pysc2 observations into a format ready for use with modelling frameworks.

## Setup

Clone the repository with all of the submodules:

```bash
git clone --recurse-submodules
```